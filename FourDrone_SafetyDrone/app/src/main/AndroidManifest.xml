<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fourdrone.safetydrone">

    // 사진 저장
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    // GPS
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    // 문자
    <uses-permission android:name="android.permission.SEND_SMS"/>
    // 진동
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar.Fullscreen">
        <activity
            android:name="com.fourdrone.safetydrone.activity.BebopActivity"
            android:screenOrientation="landscape"/>
        <activity
            android:name="com.fourdrone.safetydrone.activity.DeviceListActivity"/>
        <activity
            android:name="com.fourdrone.safetydrone.activity.IntroActivity"
            android:theme="@style/AppTheme.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>